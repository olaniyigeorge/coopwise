
[pytest]
testpaths = tests
python_files = test_*.py
pythonpath = .
asyncio_mode= auto
markers =
    unit: fast tests using fakes only, no DB/network
    integration: tests that exercise the real DB via async_client
    essential: mark test as essential to run before server start