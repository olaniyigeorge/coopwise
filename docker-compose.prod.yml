services:
  backend:
    restart: always
    command: >-
      gunicorn main:app
      -k uvicorn.workers.UvicornWorker
      --bind 0.0.0.0:8000
      --workers 4
  
  frontend:
    restart: always
    command: npm start
  
  postgres:
    restart: always
  
  redis:
    restart: always
