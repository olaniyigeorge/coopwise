{
  "scripts": {
    "dev": "concurrently -n web,api -c cyan,green \"npm run dev:web\" \"npm run dev:api\"",
    "dev:web": "cd apps/web && npm run dev",
    "dev:api": "cd backend && python3 start_backend.py",
    "install:all": "npm install && cd apps/web && npm install",
    "setup": "npm run install:all && cd backend && pip install -r requirements.txt --break-system-packages"
  },
  "dependencies": {
    "react-markdown": "^10.1.0",
    "remark-gfm": "^4.0.1"
  },
  "devDependencies": {
    "concurrently": "^8.2.0"
  }
}