# CoopWise Keep-Alive Service Configuration

# Your CoopWise backend URL (replace with your actual Render URL)
BACKEND_URL=https://coopwise.onrender.com

# Keep-alive endpoint path (will be added to BACKEND_URL)
KEEP_ALIVE_ENDPOINT=/ping

# Ping interval in minutes (should be less than 15 to prevent Render sleep)
PING_INTERVAL_MINUTES=14

# Log level (info, warn, error)
LOG_LEVEL=info

# Optional: Health check endpoint for this service
PORT=3001 